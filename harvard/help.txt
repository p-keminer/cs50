QUERY PLAN
|--SEARCH students USING INTEGER PRIMARY KEY (rowid=?)
|--SEARCH enrollments USING INDEX enrollmentsx (student_id=?)
`--SEARCH courses USING INTEGER PRIMARY KEY (rowid=?)
QUERY PLAN
|--SEARCH students USING INTEGER PRIMARY KEY (rowid=?)
`--LIST SUBQUERY 2
   |--SEARCH enrollments USING INDEX enrollmentsxx (course_id=?)
   |--SCALAR SUBQUERY 1
   |  `--SEARCH courses USING COVERING INDEX coursesx (department=? AND number=? AND semester=?)
   `--CREATE BLOOM FILTER
QUERY PLAN
|--SEARCH courses USING INDEX coursesxx (semester=?)
|--SEARCH enrollments USING COVERING INDEX enrollmentsxx (course_id=?)
`--USE TEMP B-TREE FOR ORDER BY
QUERY PLAN
`--SEARCH courses USING INDEX coursesxx (semester=?)
QUERY PLAN
|--SEARCH requirements USING INTEGER PRIMARY KEY (rowid=?)
`--SCALAR SUBQUERY 2
   |--SEARCH satisfies USING INDEX satisfiesx (course_id=?)
   `--SCALAR SUBQUERY 1
      `--SEARCH courses USING INDEX coursesxx (semester=?)
QUERY PLAN
|--SEARCH satisfies USING INDEX satisfiesx (course_id=?)
|--LIST SUBQUERY 1
|  |--SEARCH enrollments USING INDEX enrollmentsx (student_id=?)
|  `--CREATE BLOOM FILTER
|--SEARCH requirements USING INTEGER PRIMARY KEY (rowid=?)
`--USE TEMP B-TREE FOR GROUP BY
QUERY PLAN
`--SEARCH courses USING INDEX coursesxx (semester=?)
